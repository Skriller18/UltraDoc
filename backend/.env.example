# Copy to .env and fill as needed

# If set, we will use OpenAI for answering/extraction.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Embedding model (OpenAI only for now)
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# Storage
STORAGE_DIR=storage
CHROMA_DIR=storage/chroma

# Guardrails
MIN_SIMILARITY=0.35
TOP_K=6
